
To run this project, you will have to install the "Live Server" by ritwick dey extension from vscode extensions. 

then go to the bottom right of your screen and click the "Go Live" button.

This is because my website project uses an implementation of the google maps API. This will fail to load because of CORS-related security issues unless you do this. 

The google analytics will also show some errors if you run it incorrectly as it will not be able verify the origin of the data as being from my website.






This project will be privated by 30/06/2026 and the maps API key invalidated.
